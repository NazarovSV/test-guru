<h2><%= @test.title %>:</h2>
<% if @questions.present? %>
  <table>
    <tr>
      <th align="left">Questions:</th>
      <th colspan="3"></th>
    </tr>
    <%= render @questions %>
  </table>
<% end %>
<br/>
<br/>

<%= link_to 'New Question', new_test_question_url(test_id: @test.id) %> |
<%= link_to 'Back', admin_tests_path %>