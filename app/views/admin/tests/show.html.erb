<h2><%= @test.title %>:</h2>
<% if @questions.present? %>
  <table>
    <tr>
      <th align="left"><%= t('.questions') %>:</th>
      <th colspan="3"></th>
    </tr>
    <%= render @questions %>
  </table>
<% end %>
<br/>
<br/>

<%= link_to t('.new'), new_admin_test_question_url(test_id: @test.id) %> |
<%= link_to t('.back'), admin_tests_path %>