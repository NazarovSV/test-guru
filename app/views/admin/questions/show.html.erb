<h3>
  <%= @question.body %>
</h3>
<h2><%= t('.header') %></h2>
<%= link_to t('.new'), new_admin_question_answer_path(@question) %>

<ol>
  <%= render @question.answers %>
</ol>

<br>
<%= link_to t('button.edit'), edit_admin_question_path(@question) %> |
<%= link_to t('button.back'), admin_test_path(@question.test) %>