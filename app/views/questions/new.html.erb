<h1>Create new question:</h1>

<%= form_with url: "/tests/#{params[:test_id]}/questions", method: :post do |form| %>
  <p>
  <%= form.label :question, "Question:" %>
  <%= form.text_field "question[body]", id: :question %>
  </p>
  <%= form.submit "Create question" %>
<% end %>