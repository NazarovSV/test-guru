<h2><%= @test.title %>:</h2>
<% if @questions.present? %>
  <table>
    <tr>
      <th align="left">Questions:</th>
      <th colspan="3"></th>
    </tr>
    <%= render @questions %>
  </table>
<% end %>
<br/>
<br/>

<nav aria-label="breadcrumb">
  <ol class="breadcrumb justify-content-center">
    <li class="breadcrumb-item"><%= link_to 'New Question', new_test_question_url(test_id: @test.id) %> |</li>
    <li class="breadcrumb-item"><%= link_to 'Back', tests_url %></li>
  </ol>
</nav>